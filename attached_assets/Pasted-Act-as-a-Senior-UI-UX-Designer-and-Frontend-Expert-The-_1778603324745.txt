Act as a Senior UI/UX Designer and Frontend Expert. The user wants to transform the campaign analytics tables from a basic layout into a high-end, professional SaaS dashboard (similar to TripleWhale or Northbeam).

Please rewrite the Table components globally using this "Elite SaaS UI" framework:

1. LUXURY TYPOGRAPHY & SPACING

Fonts: Use 'Cairo' or 'Readex Pro' for Arabic text and a clean Sans-Serif for numbers/English IDs.

Vertical Rhythm: Increase row height with generous padding (py-5 px-6).

Font Sizes: Headers should be text-[11px] uppercase tracking-widest text-slate-400 font-bold. Row data should be text-sm text-slate-700.

2. DATA VISUALIZATION & BADGES (The "Pro" Look)

Status Badges: Instead of plain text for decisions:

Winner/Scale: Pill-shaped badge bg-emerald-50 text-emerald-600 border border-emerald-100.

Stop/Low Performance: Pill-shaped badge bg-rose-50 text-rose-600 border border-rose-100.

Wait/Learning: Pill-shaped badge bg-amber-50 text-amber-600 border border-amber-100.

Metric Highlighting: Make the primary metric (e.g., ROAS or CPA) slightly bolder or a different color to draw the eye immediately.

3. ADVANCED ARCHITECTURE (RTL/LTR Balance)

Table Wrapper: Use rounded-2xl border border-slate-200 overflow-hidden shadow-sm bg-white.

RTL Force: The table is dir="rtl".

LTR Containment: For Campaign Names and IDs, use:

<div class="flex flex-col items-start gap-1">
  <span class="font-bold text-slate-900 truncate max-w-[280px]" dir="ltr" title="{item.name}">{item.name}</span>
  <span class="text-[10px] font-mono text-slate-400 bg-slate-50 px-1.5 py-0.5 rounded" dir="ltr">{item.id}</span>
</div>


Numbers Alignment: Align all numeric data to the LEFT (LTR) within their cells so decimals line up perfectly.

4. INTERACTIVE FEATURES

Zebra & Hover: Use even:bg-slate-50/30. On hover, use hover:bg-indigo-50/40 transition-all duration-200.

Sticky Header: The header must have a backdrop-blur-md bg-white/80 sticky top-0 border-b border-slate-200 z-30.

Empty States: If no data is found, show a beautiful illustrated empty state instead of an empty table.

5. IMPLEMENTATION CHECK

Ensure no | characters are displayed; if they exist in the raw name, hide or strip them in the UI rendering.

Ensure the horizontal scroll is smooth with a custom thin scrollbar.

Please implement this high-fidelity design globally to make the analytics dashboard look world-class.
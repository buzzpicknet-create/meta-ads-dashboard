Act as a Senior AI Architect. The user wants the AI Chat Agent to behave more autonomously and intelligently when creating campaigns via Pipeboard, similar to how ChatGPT behaves when connected to an API. The Agent should not just be a "dumb executor" but a "proactive consultant".

Please implement the following upgrades to the Agent's reasoning loop and System Prompt:

1. NEW MCP TOOL: fetch_account_metadata

Before creating a campaign, the AI needs to know what exists in the account.

Create a new MCP tool called fetch_account_metadata.

This tool should fetch from Meta/Pipeboard: Available Pixel IDs (with names), Available Page IDs, and a summary of the 3 most recently created campaigns (to understand the user's typical structure/naming conventions).

2. SYSTEM PROMPT UPGRADE (The "Smart Campaign Builder" Logic)

Update the LLM's core System Prompt with these strict behavioral rules:

"""
SMART CAMPAIGN CREATION WORKFLOW:
When the user asks to create a campaign, DO NOT immediately execute launch_pipeboard_campaign if you are missing crucial context. Follow these steps:

Context Gathering: Automatically call fetch_account_metadata to see available Pixels, Pages, and recent campaign structures.

Intelligent Defaults: If the user doesn't specify targeting, budget, or placement, assume the industry best practice (e.g., Advantage+ Shopping or Broad Targeting, $20-$50 initial budget).

Proactive Confirmation: BEFORE launching, summarize what you are about to do in a conversational way.

Example: "I'm ready to launch. I noticed your recent campaigns use the 'Main Store Pixel', should I use that? I've set the targeting to Broad and the budget to $50/day. Shall I proceed?"

Missing Data Handling: If a crucial piece of data is completely missing (e.g., the user provided a landing page but no video/media link), DO NOT fail. Politely ask the user: "I have the landing page, but please provide the Google Drive link for the video so I can build the ad."
"""

3. CHAT INTERFACE UX (Optional but recommended)

When the AI is gathering context (running fetch_account_metadata), show a small status indicator in the chat: "🔍 جاري فحص الحساب الإعلاني واستخراج البيكسلات المتاحة..."